# Simple To-Do List

Local preview:
- Open `index.html` in browser or use VSCode Live Server.
- Tasks persist using `localStorage`.

Testing steps:
1. Add a task and press Enter.
2. Click on a task to toggle complete.
3. Double-click to remove.

Notes:
- No backend; `.env` is ignored.
